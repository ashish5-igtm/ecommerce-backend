spring.application.name=ecommerce

# Replace <YOUR_MYSQLHOST_HERE> with the revealed value from the MYSQLHOST variable in Railway
spring.datasource.url=jdbc:mysql://mysql.railway.internal:3306/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# Credentials go here, NOT in the URL
spring.datasource.username=root
spring.datasource.password=FihxurmHGKgMqAMwOJzuLioGDnlEcZXL

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Fixed the syntax error and set fallback port to standard 8080
server.port=${PORT:8080}
